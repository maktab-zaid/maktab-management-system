/**
 * googleSheets.ts — DELETED. CSV parser and Google Sheets integration removed.
 * This file is intentionally empty. Remove any imports from this path.
 */

/** @deprecated Use storage.ts directly. Kept so old imports compile. */
export function normalizeMobile(mobile: string): string {
  return mobile.replace(/\D/g, "");
}
